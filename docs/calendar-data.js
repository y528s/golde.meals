/* PLACEHOLDER — replaced by `npm run calendar`.

   Empty on purpose. With no verified dates, goldeCalendar answers null to
   everything and the app says nothing about the parsha, yom tov or fasts.
   That is the correct behaviour: a wrong parsha reads as an outsider guessing,
   and silence never does.

   Run `npm run calendar -- --year 2026 --geonameid <your city>` to fill it. */
goldeCalendar.load({}, "", "");
